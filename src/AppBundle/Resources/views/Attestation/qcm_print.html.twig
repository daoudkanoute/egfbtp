{% extends 'AppBundle::layout.html.twig' %}
{% block title %}Passeport Sécurité Intérim{% endblock %}

{% block headerscript %}
    <style>
        .title-condidate {
            font-size: 14px;
            margin-top: 5px;
            border-bottom: 1px solid #efefef;
            color: #4F87C6;
        }
        .has-image {
            text-align: center;
            padding-bottom: 25px;
        }
        .has-image label {
            text-align: center;
            padding: 7px 10px;
            display: block;
            margin-bottom: 2px;
        }
        .has-image label:before {
            left: 50%;
            bottom: 0;
            top: 95%;
        }
        .radio-custom.has-image input[type="radio"]:checked + label:after {
            top: 0;
            left: 5px;
            bottom: 0;
            margin-top: 6px;
        }
        .checkbox-custom input[type="checkbox"]:checked + label:after {
            top: 0;
            left: 3px;
            bottom: 0;
            margin-top: 6px;
        }
        html.fixed .content-body { margin-left: 0; }
        html.fixed .header, html.fixed .sidebar-left, html.fixed .page-header, .sound-player { display: none; }
        html.fixed .inner-wrapper { padding: 0; }
    </style>
{% endblock %}

{% block breadcrumbs %}
    <li><span>{{ block('title') }}</span></li>
{% endblock %}

{% block body %}
    <section class="panel">
        <div class="panel-body">
            <div class="title-condidate">
                <b>Nom & prénom: </b>{{ apprenant.nom~' '~apprenant.prenom }}<br/>
                <b>Code: </b>{{ apprenant.code }}
            </div>
            <h1 class="text-center">Passport Sécurité Intérim</h1>
            <div class="text-center"><b> Pour réussir au test, il faut obtenir <span class="label-success">&nbsp;70 %&nbsp;</span> de bonnes réponses minimum, soit 18 bonnes réponses sur un total de 26 questions.</b></div>
            <hr/>
            <div class="col-md-12">
                <div class="col-md-12">
                {{ form_start(form, {'attr':{'class': 'form-horizontal form-bordered','novalidate': 'novalidate'}}) }}
                {% form_theme form 'AppBundle:Attestation:_widgets_print.html.twig' %}
                {{ form(form) }}
                </div>
            </div>
        </div>
    </section>
    <script>window.print();</script>
{% endblock body %}